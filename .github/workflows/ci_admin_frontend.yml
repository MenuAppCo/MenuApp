name: CI Admin frontend 

on:
  push:
    branches: [ master ]
    paths:
      - 'admin-frontend/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'admin-frontend/**'

jobs:
  ci-admin-frontend:
    uses: ./.github/workflows/reusable-frontend-ci.yml
    with:
      frontend-path: 'admin-frontend'
      node-version: '20'